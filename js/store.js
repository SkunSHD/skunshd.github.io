/* */(function (window) {	'use strict';		/**	* Create a new client side storage object	*	*/	function Store() {	}		/**	* Get month, year or event from localStorage here	*	* @param {string} or {object} item The key that help find data in localString	*/	Store.prototype.get = function (item) {				if (!item) {			return;		}		var result = '';		switch (typeof item) {			case 'string':			result = localStorage.getItem(item);			break;			case 'object':			var itemInString = window.app.Methods.formatDate(item);			result = localStorage.getItem(itemInString);			break;		}		return result;	};		Store.prototype.set = function (key, value) {		if (!item || !value) {			return;		}		localStorage.setItem(key, value);	}		window.app = window.app || {};	window.app.Store = Store;})(window);