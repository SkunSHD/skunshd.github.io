/* */(function (window) {	'use strict';		/**	* Create a new client side storage object	*	*/	function Store() {	}		Store.prototype.get = function (item) {				if (!item) {			return;		}		var result = '';		switch (typeof item) {			case 'string':			result = localeStorage.getItem(item);			break;			case 'object':			var itemInString = window.app.Methods.formatDate(item);			result = localeStorage.getItem(itemInString);			break;		}	};		window.app = window.app || {};	window.app.Store = Store;})(window);